spring:
  application:
    name: indoor-vps-v2
  profiles:
    default: local
  servlet:
    multipart:
      max-file-size: ${MULTIPART_MAX_FILE_SIZE:2GB}
      max-request-size: ${MULTIPART_MAX_REQUEST_SIZE:2GB}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate

server:
  port: ${SERVER_PORT:8080}

springdoc:
  swagger-ui:
    path: /docs
  api-docs:
    path: /openapi.json

indoor:
  python:
    executable: ${PYTHON_EXECUTABLE:python3}
    bridge-script: ${PYTHON_BRIDGE_SCRIPT:./scripts/python_bridge/bridge_entry.py}
    backend-source: ${PYTHON_BACKEND_SRC:./python/legacy_backend/src}
    timeout-seconds: ${PYTHON_BRIDGE_TIMEOUT_SECONDS:60}
    build-superpoint-index-timeout-seconds: ${PYTHON_BRIDGE_BUILD_SP_TIMEOUT_SECONDS:600}
    daemon:
      enabled: ${PYTHON_BRIDGE_DAEMON_ENABLED:true}
  rtabmap:
    reprocess:
      enabled: ${RTABMAP_REPROCESS_ENABLED:true}
      executable: ${RTABMAP_REPROCESS_BIN:rtabmap-reprocess}
      timeout-seconds: ${RTABMAP_REPROCESS_TIMEOUT_SECONDS:300}
  build-worker:
    enabled: ${BUILD_WORKER_ENABLED:true}
    poll-interval-ms: ${BUILD_WORKER_POLL_INTERVAL_MS:2000}
    batch-size: ${BUILD_WORKER_BATCH_SIZE:1}
  poi-labeler:
    enabled: ${POI_LABELER_ENABLED:false}
    executable: ${POI_LABELER_PYTHON:python3}
    working-dir: ${POI_LABELER_DIR:./python/tools/poi_labeler}
    entry-script: ${POI_LABELER_ENTRY:label_pois.py}
    timeout-seconds: ${POI_LABELER_TIMEOUT_SECONDS:600}
